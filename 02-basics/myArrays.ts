const superheros: string[] = []
const powers: number[] = []
const anotherPowers: Array<number> = []

superheros.push("spiderman")
powers.push(2)

const mlModels: number[][] = [];
mlModels.push([1, 2, 4, 5, 6])
mlModels.push([0, 1])
