// tuple maintains a order
// [] in type means a tuple
type ApiResponse = [string, number, boolean];

let apiResponse1: ApiResponse = ["1", 1, true];
// let apiResponse2: ApiResponse = [1, 1, true] // error due to invalid order

type RGB = [number, number, number];

export {};
